# /merge

## 概要
現在のブランチをmainブランチにマージします。

## 動作
1. 現在のブランチを確認
2. コミットされていない変更がないか確認
3. mainブランチに切り替え
4. `git pull origin main` で最新化
5. 元のブランチをマージ
6. 成功したらmainをプッシュ

## 使用例
```
/merge
```

## 注意事項
- mainブランチへの直接マージは慎重に行う
- コンフリクトが発生した場合は手動解決が必要
- 重要な変更の場合は、事前にレビューを推奨
